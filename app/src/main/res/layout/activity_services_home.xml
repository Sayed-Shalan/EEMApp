<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <!-- Services Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="0.1"
        android:background="@color/mycolorPrimary"
        android:orientation="horizontal">

        <!-- Image Report -->
        <ImageView
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="0.15"
            android:contentDescription="TODO" android:src="@drawable/ic_report" />

        <!-- Services Home Title -->
        <TextView
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            android:layout_weight="0.7"
            android:gravity="center"
            android:text="test"
            android:textColor="@color/txtTitleColor"
            android:textSize="@dimen/txtHeader" />

        <!-- Image Logout -->
        <ImageView
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="0.15"
            android:contentDescription="TODO" android:src="@drawable/ic_logout" />

    </LinearLayout>

    <!-- Services Tabs -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="0.1"
        android:background="@color/mycolorPrimary"
        android:orientation="horizontal">

        <!-- Image Appointment -->
        <ImageView
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="0.33"
            android:contentDescription="TODO" android:src="@drawable/ic_appointment" />

        <!-- Image Offer -->
        <ImageView
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="0.34"
            android:contentDescription="TODO" android:src="@drawable/ic_offer" />

        <!-- Image Data -->
        <ImageView
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="0.33"
            android:contentDescription="TODO" android:src="@drawable/ic_data" />

    </LinearLayout>

    <FrameLayout
        android:id="@+id/fServicesLayout"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="0.8" />

</LinearLayout>